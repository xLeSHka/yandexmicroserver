# config/local.yaml

env: "local" # Окружение - local, dev или prod
storage_path: "./backend/storage/storage.db"
http_server: # конфигурация нашего http-сервера
  address: "localhost:8080"
  timeout: 4s
  idle_timeout: 30s
storage:
  username: "user123"
  host: "localhost"
  port: "8080"  
  database: "postgres" 
  password: "password123"
  maxattempts: 5